{
  "schema_version": 2,
  "name": "input.key",
  "version": "1.0.0",
  "entry": "builtin:KeyAction",
  "description": "发送 Android keyevent，例如 BACK、ENTER、HOME。",
  "parameters": { "keycode": { "type": "string", "required": true, "min_length": 1 } },
  "outputs": { "type": "object", "properties": { "keycode": { "type": "string" } }, "additionalProperties": false },
  "effects": { "side_effect": true, "retry": "unsafe" }
}
